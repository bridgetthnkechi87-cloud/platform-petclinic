apiVersion: v1
kind: Service
metadata:
  name: visits-service
  labels:
    app.kubernetes.io/name: visits-service
    app.kubernetes.io/part-of: petclinic
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: visits-service
    app.kubernetes.io/part-of: petclinic
  ports:
    - name: http
      port: 8082
      targetPort: http