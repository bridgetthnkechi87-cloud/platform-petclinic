# Visits Service Raw Manifests

This folder contains the raw Kubernetes Deployment and Service for
`visits-service`.

The active deployment is still the
`visits-service-dev` or `visits-service-prod` Argo CD Application, which renders
`helm/petclinic-service` with `helm-values/visits-service.yaml`.