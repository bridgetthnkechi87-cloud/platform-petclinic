apiVersion: v1
kind: Service
metadata:
  name: customers-service
  labels:
    app.kubernetes.io/name: customers-service
    app.kubernetes.io/part-of: petclinic
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: customers-service
    app.kubernetes.io/part-of: petclinic
  ports:
    - name: http
      port: 8081
      targetPort: http